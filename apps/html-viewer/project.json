{
  "name": "@digests/html-viewer",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx vite build",
        "cwd": "apps/html-viewer"
      },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
